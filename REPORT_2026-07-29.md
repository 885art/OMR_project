# 2026-07-29 OMR 進度報告

## 一句話摘要

目前已完成官方 YOLOv9-S 的 40 類樂譜符號模型，以及 slur/tie 獨立模型的資料與訓練流程。
DeepScores validation 表現很好，但真實 Beethoven 樂譜顯示 YOLOv9 的 staccato 召回率低於原本
YOLO11，證明 synthetic validation 與 real-score performance 之間仍有 domain gap。

## 1. 專案目標

原始 25-OMR 使用 OEMER segmentation 與規則辨識音符、staff 和節奏，但 articulation、
dynamic、ornament、hairpin、slur 和 tie 不完整。

目前擴充流程：

1. OEMER／原始 25-OMR 產生音符、staff 與小節結構。
2. YOLO 在 1024×1024 overlapping tiles 上偵測符號。
3. 合併重疊 tile detections。
4. 根據 staff、音符位置、方向與距離做規則配對。
5. 將結果寫入 MusicXML。

## 2. 這次完成的工作

- 使用老師指定的官方 `WongKinYiu/yolov9`。
- 選用 YOLOv9-S，因為本機 RTX 3060 只有 8GB VRAM。
- 建立 40 類 symbol detector。
- 另外建立 2 類 slur/tie detector，避免大量曲線資料壓過其他符號。
- 加入 PyTorch 2.7 checkpoint 相容處理與 tiled inference adapter。
- slur/tie 採用「YOLO 曲線候選 + 音高／staff／端點規則」，不是只相信 YOLO 類別。
- 建立 preflight、training、resume 與 dataset validation 流程。
- 程式已推送到 `feature/yolov9-migration`。

## 3. Dataset 與訓練設定

### 40 類 symbol

- train：8,836 tiles
- validation：2,323 tiles
- 類別包含 articulation、dynamic letters、ornament、pedal、fingering、tremolo、hairpin 等。

### Slur/tie

- train：9,573 tiles
- validation：2,427 tiles
- 共 12,000 tiles、42,855 個 tile instances
- DeepScores 原始資料有 4 個零面積曲線框，已明確記錄並排除。

### 訓練參數

- Model：YOLOv9-S
- Image size：1024
- Batch size：2
- Epochs：60
- Optimizer：AdamW
- GPU：NVIDIA RTX 3060 8GB
- Tile overlap：256 pixels

## 4. 40 類模型 validation 結果

最佳結果約在 epoch 54：

| Metric | Result |
|---|---:|
| Precision | 0.978 |
| Recall | 0.952 |
| mAP@0.5 | 0.980 |
| mAP@0.5:0.95 | 0.887 |

訓練曲線：

`articulation_experiments/outputs/runs/yolov9_symbols_v1/results.png`

Confusion matrix：

`articulation_experiments/outputs/runs/yolov9_symbols_v1/confusion_matrix.png`

## 5. Beethoven 真實樂譜測試

相同 Beethoven 頁面、相同 staccato threshold 0.45：

| Page | YOLOv9 candidates | YOLOv9 matched | YOLOv9 rejected | YOLOv9 staccato | YOLO11 candidates | YOLO11 matched | YOLO11 staccato |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 79 | 55 | 24 | 17 | 143 | 118 | 78 |
| 2 | 59 | 47 | 12 | 24 | 92 | 82 | 61 |
| 3 | 87 | 47 | 40 | 15 | 117 | 83 | 50 |
| 4 | 122 | 71 | 51 | 31 | 185 | 129 | 95 |
| Total | 347 | 220 | 127 | 87 | 537 | 412 | 284 |

YOLOv9 四頁另外偵測到 229 個 dynamic candidates，數量偏多，表示 real-score false positives
仍需要處理。

### 結果解讀

- YOLOv9 migration 在工程上成功，模型、推論與 MusicXML pipeline 都能執行。
- DeepScores validation 很高，但不代表真實掃描譜也同樣準確。
- YOLOv9 在四頁只偵測 87 個 staccato，舊 YOLO11 為 284 個，存在明顯 regression。
- 後處理拒絕了 127/347 個候選，顯示 YOLO + music rules 的 hybrid approach 有必要。
- 目前不能宣稱 YOLOv9 優於 YOLO11；YOLO11 暫時仍是 symbol detector 的較佳實用版本。

### 可能原因

- DeepScores 與 Henle Beethoven 掃描譜存在 domain gap。
- staccato training bbox 中位數約 6×6 pixels，屬於極小物件。
- YOLO11 與 YOLOv9 的 confidence calibration 不同，不能假設同一 threshold 等價。
- YOLOv9 dual-head 與現有 hyperparameters 尚未針對 OMR tiny objects 調整。
- Albumentations 在官方 YOLOv9 環境中出現版本相容警告，額外 augmentation 沒有啟用。

## 6. Slur/tie 目前狀態

截至 2026-07-28 凌晨，訓練完成 epoch 29/60：

| Metric | Interim result |
|---|---:|
| Precision | 0.982 |
| Recall | 0.956 |
| mAP@0.5 | 0.986 |
| mAP@0.5:0.95 | 0.899 |

這是 DeepScores validation 的中途結果，訓練仍在進行，還沒有完成真實樂譜評估。

## 7. 下一步

1. 完成 slur/tie 60 epochs，測試重疊曲線、長 slur 與 tie。
2. 建立人工標註的 real-score test set，不能只看 DeepScores validation。
3. 對 YOLO11／YOLOv9 使用相同真實頁面做 per-class precision、recall 比較。
4. 重新校正每一類 confidence threshold。
5. 擴張 staccato tiny bbox，測試更高解析度或針對小物件的 crop。
6. 加入真實樂譜與 hard negatives 做 fine-tuning。
7. YOLOv9 尚未改善前，實際 symbol pipeline 保留 YOLO11。

## 建議口頭講稿（約 5 分鐘）

> 這一階段的目標，是把原本只能處理基本音符和節奏的 25-OMR，擴充到 articulation、
> dynamics、ornaments、hairpins 以及 slur/tie。方法不是完全取代 OEMER，而是在原本
> 音符結構上加一個 YOLO symbol detector，再用 staff、方向、距離和音高規則，把偵測框
> 配對到 MusicXML。
>
> 依照老師的建議，我把原本的 YOLO11 pipeline 遷移到官方 YOLOv9-S。符號模型有 40 類，
> slur/tie 另外訓練兩類模型。40 類模型在 DeepScores validation 的 mAP50 約 0.98，
> mAP50 到 95 約 0.887。
>
> 不過實際測試 Beethoven 後發現重要問題。四頁中，YOLO11 找到 284 個 staccato，
> YOLOv9 只有 87 個；而且 YOLOv9 的 dynamic candidates 偏多。因此目前的結論不是
> YOLOv9 一定比較好，而是 migration 已成功，但真實樂譜發生 performance regression。
> 這反映 DeepScores 和真實掃描譜之間的 domain gap，以及 staccato 只有大約 6×6 pixels
> 的 tiny-object 問題。
>
> 下一步會建立真實樂譜標註集，重新調整 class thresholds、tiny-object training bbox
> 與 augmentation，並以相同 real-score test set 比較 YOLO11 和 YOLOv9。在 YOLOv9
> 改善以前，實際 symbol detector 會保留表現較好的 YOLO11。slur/tie 模型則仍在訓練，
> 中途的 DeepScores mAP50 約 0.986，完成後再進行真實曲線測試。

## 報告前準備清單

- 開啟 40 類模型 `results.png`。
- 開啟 40 類模型 `confusion_matrix.png`。
- 開啟 YOLO11 Beethoven 第 1 頁標註圖。
- 開啟 YOLOv9 Beethoven 第 1、2、3、4 頁標註圖。
- 預先用 MuseScore 開啟一份 `.with_articulations.musicxml`，不要現場才載入。
- 準備 YOLO11／YOLOv9 四頁比較表。
- 準備一張流程圖：PDF → OEMER → YOLO → music rules → MusicXML。
- 強調 validation 與 real-world performance 不同。
- 不要宣稱 YOLOv9 已優於 YOLO11。
- 若要 live demo，先保留標註 JPG 當備援，避免現場模型或 GPU 發生問題。
